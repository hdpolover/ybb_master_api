-/admins (isinya foto profil admin)
-/programs (isinya buat data-data program yang berbentuk file berdasarkan id)
	-/{$program-id}
-/payment-methods (isinya gambar metode pembayaran untuk semua program)
-/payments (isinya semua bukti pembayaran berdasarkan program id)
	-/{$program-id}
-/program-photos (isinya semua foto program berdasarkan program category id)
	-/{$program-category-id}
-/testimonies (isi semua foto testimoni berdasarkan program id)
	-/{$program-id}
	-/{$program-id}
-/participants (isinya semua data participant berdasarkan program id)
	-/{$program-id}
		-/{$uid}
			-/pictures
			-/documents
-/sponsors (isinya semua foto sponsor)
-/web-setting-home (isinya semua foto atau file untuk setting home berdasarkan program id}
	-/{$program-id}
-/certificates (isinya semua template sertifikat berdasarkan program id)
	-/{$program-id}
-/announcements (isinya semua poster pengumuman berdasarkan program id)
	-/{$program-id}
-/general-files (untuk kebutuhan file web)
	